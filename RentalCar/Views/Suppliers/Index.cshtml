@using RentalCar.DomainLayer.DTO
@model List<SupplierDTO>
@{
    Layout = "_AdminLTELayout";
    ViewData["Title"] = "Suppliers";
    var isRtl = (bool?)ViewData["IsRtl"] ?? false;
}

@section Styles {
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" />
    <style>#supTable th, #supTable td { text-align:center !important; vertical-align:middle; }</style>
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6"><h1 class="m-0"><i class="fas fa-truck mr-2"></i>Suppliers</h1></div>
            <div class="col-sm-6">
                <ol class="breadcrumb @(isRtl ? "float-sm-left" : "float-sm-right")">
                    <li class="breadcrumb-item"><a href="/Admin/Index">Home</a></li>
                    <li class="breadcrumb-item active">Suppliers</li>
                </ol>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container-fluid">
        @if (ViewBag.ErrorMessage != null) { <div class="alert alert-danger">@ViewBag.ErrorMessage</div> }
        @if (TempData["Success"] != null) { <div class="alert alert-success">@TempData["Success"]</div> }
        @if (TempData["Error"] != null) { <div class="alert alert-danger">@TempData["Error"]</div> }

        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h3 class="card-title mt-1"><i class="fas fa-list mr-1"></i> All Suppliers</h3>
                <a class="btn btn-success btn-sm" href="/Suppliers/Create"><i class="fas fa-plus-circle mr-1"></i> Add</a>
            </div>
            <div class="card-body">
                <table id="supTable" class="table table-striped table-bordered nowrap" style="width:100%">
                    <thead><tr><th>#</th><th>Name</th><th>Name (Arabic)</th><th>Email</th><th>Phone</th><th>Action</th></tr></thead>
                    <tbody>
                        @{ var count = 1; }
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@count</td>
                                <td>@(string.IsNullOrEmpty(item.Name) ? "-" : item.Name)</td>
                                <td>@(string.IsNullOrEmpty(item.Name_ar) ? "-" : item.Name_ar)</td>
                                <td>@(string.IsNullOrEmpty(item.Email) ? "-" : item.Email)</td>
                                <td>@(string.IsNullOrEmpty(item.Phone) ? "-" : item.Phone)</td>
                                <td>
                                    <a class="btn btn-sm btn-warning" href="/Suppliers/Edit/@item.Id"><i class="fas fa-edit"></i></a>
                                    <form method="post" asp-action="Delete" asp-controller="Suppliers" class="d-inline"
                                          onsubmit="return confirm('Delete this supplier?');">
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <button type="submit" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
                                    </form>
                                </td>
                            </tr>
                            count++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script>
        $(function () {
            $('#supTable').DataTable({ responsive: true, pageLength: 10, lengthMenu: [5,10,25,50],
                language: { emptyTable: "No suppliers found" }, columnDefs: [{ orderable:false, targets:[5] }] });
        });
    </script>
}
